<h1>Editing '<%= @category.category_name %>' category</h1>

<%  if @category.photo.exists? then %>
    <%= image_tag(@category.photo.url, :class => 'showProduct') %>
<% end %>

<%= render 'form' %>


<table class="nav_icons">
  <tr>
    <td><%= link_to image_tag('icons/back.png', :alt => 'Back', :title => 'Back'), categories_path %></td>
    <td><%= link_to image_tag('icons/show.png', :alt => 'Show', :title => 'Show'), @category %></td>
  </tr>
</table>
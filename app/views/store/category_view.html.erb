<h1>Product Categories</h1>
<% @categories.each do |category| %>
    <%= link_to image_tag(category.photo.url(:store), :alt => category.category_name, :title => category.category_name) + category.category_name, {:controller => "store", :action => "index", :category => category.id }, :class => "category_link" %>
<% end %>